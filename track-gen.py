import numpy as np
import matplotlib as plt
N = 100

class Disturbance(track, d):

    for i in range[N]:

        if i%3700:
            dist = [t[i],d[1]]
            print("here")

    # return dist


dist = 0
len = 10
depth = 10
d = [len, depth]

track = np.linspace(0, 15200 , num=N)

# disturbance = np.zeros(N)

dist = Disturbance(track, d)
print(tracklen)
